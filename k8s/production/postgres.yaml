apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres
  namespace: trading-system-production
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      labels:
        app: postgres
    spec:
      containers:
        - name: postgres
          image: postgres:15-alpine
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_USER
              value: trader
            - name: POSTGRES_PASSWORD
              value: trader123
            - name: POSTGRES_DB
              value: trading
---
apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: trading-system-production
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
